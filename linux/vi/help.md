## Replace all occurrences of “OLD” on current line with “NEW” using /g

```vi
:s/OLD/NEW/g
```

## Replace every occurrence of “OLD” with “NEW” within the file using :%s, for example

```vi
:%s/OLD/NEW/g
```
